'use client';
import {Move,RotateCcw,FlipHorizontal2,FlipVertical2,AlignHorizontalJustifyCenter,AlignVerticalJustifyCenter} from 'lucide-react';
import {useEditor} from '../state/editor';
import {NumberField,Toggle} from './Fields';
import {assetById} from '../library';
import {centroid,type Paths} from '../geometry/polygons';
export type TargetRegion='base';
export function Properties({region}:{region:Paths;target:TargetRegion;setTarget:(x:TargetRegion)=>void}){const s=useEditor(),o=s.project.ornaments[0],c=centroid(region);const change=(patch:Partial<NonNullable<typeof o>>)=>s.commit(p=>{if(p.ornaments[0]&&!p.ornaments[0].locked&&!p.layers.ornaments.locked)Object.assign(p.ornaments[0],patch);});return <><p className="eyebrow">РИСУНОК НА КОНВЕРТЕ</p><h3>{o?assetById(o.assetId).name:'Выберите композицию'}</h3><div className="whole-pattern-hint"><Move size={21}/><p>Потяните рисунок на конверте — всё полотно двигается целиком.</p></div>{o&&<><div className="section-label">Крупность рисунка</div><NumberField label="Масштаб орнамента" value={o.scaleX*100} min={50} max={200} unit="%" step={5} onChange={v=>change({scaleX:v/100,scaleY:v/100})}/><p className="note">Больше масштаб — крупнее цветы и завитки.</p><div className="section-label">Положение</div><div className="field-grid"><NumberField label="Сдвиг X" value={o.x-c.x} onChange={v=>change({x:c.x+v})}/><NumberField label="Сдвиг Y" value={o.y-c.y} onChange={v=>change({y:c.y+v})}/></div><NumberField label="Поворот рисунка" value={o.rotation} min={-180} max={180} unit="°" step={1} onChange={v=>change({rotation:v})}/><div className="mirror-grid"><button title="Центр по горизонтали" onClick={()=>change({x:c.x})}><AlignHorizontalJustifyCenter size={17}/>Центр X</button><button title="Центр по вертикали" onClick={()=>change({y:c.y})}><AlignVerticalJustifyCenter size={17}/>Центр Y</button><button onClick={()=>change({flipX:!o.flipX})}><FlipHorizontal2 size={17}/>Зеркало X</button><button onClick={()=>change({flipY:!o.flipY})}><FlipVertical2 size={17}/>Зеркало Y</button></div><button className="outline full reset-pattern" onClick={()=>change({x:c.x,y:c.y,rotation:0,scaleX:1,scaleY:1,flipX:false,flipY:false})}><RotateCcw size={15}/> Исходное положение</button><div className="panel-map"><b>5 частей · 4 сгиба</b><p>1 — основание<br/>2, 3 — боковые лепестки<br/>4 — нижний<br/>5 — верхний, закрывающий</p><small>Посмотрите, как они складываются, в режиме «3D и складывание».</small></div><details className="technical-details"><summary>Слои рисунка</summary><Toggle label="Нижний PLA" checked={o.bottom} onChange={()=>change({bottom:!o.bottom})}/><Toggle label="Верхний PLA" checked={o.top} onChange={()=>change({top:!o.top})}/></details></>}<div className="inspector-footer">Орнамент обрезается по конверту автоматически</div></>;}
